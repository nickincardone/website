export interface Image {
  id: number;
  type: number;
  src: string;
  altText: string;
  location: string;
  date: string;
}

const images: Image[] = [
  // ...existing code from images.js...
];

export default images;
